function result(a, b) {
    let pus = (a + b);
    a = document.getElementById('a');
    b = document.getElementById('b');

}


function plus() {
    if ("a + b") {
        let res = a + b;
    }
    else if ("a - b") {
        let  res = a - b;
    }
}
